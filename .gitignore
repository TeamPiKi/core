# macOS
.DS_Store

# Claude Code
.claude/settings.local.json
.claude/worktrees/
# infra(TeamPiKi/infra)가 SessionStart 에 설치하는 공용 스킬 — 정본은 infra 이고 여기선 설치본이라
# 추적하지 않는다. 파일 단위로 지정해 나머지 로컬 스킬(pre-pr 등)은 계속 추적한다.
.claude/commands/commit.md
.claude/commands/gc.md
.claude/commands/coderabbit.md
.claude/commands/pr.md
.claude/commands/issue.md
.claude/commands/session-check.md
.claude/commands/session-close.md
.claude/rules/testing-principles.md

# infra(TeamPiKi/infra) 체크아웃 자리 — 계약 카탈로그(contracts/*.yaml)를 로컬은 install.sh 가, CI 는 ci.yml 의
# 체크아웃 스텝이 여기 둔다. 정본은 infra 이므로 이 repo 는 사본을 추적하지 않는다.
shared-infra/

# IDE - 개인 설정 제외
.idea/*
!.idea/codeStyles/
!.idea/inspectionProfiles/
.idea/misc.xml
.idea/workspace.xml
.idea/tasks.xml
.idea/shelf/
*.iml

# Gradle
.gradle/
build/

# IntelliJ 컴파일 출력
out/

# Environment — .env* 전부 무시하되 템플릿(.env.example)은 커밋 유지
.env*
!.env.example

# Kotlin
.kotlin/

# superpowers AI 워크플로 산출물 (로컬 전용) — 로컬 세팅 차이로 새어나가지 않도록 레포에서 무시
docs/superpowers/

# 링크 검수(LinkScreening*E2ETest) 입력 — SCREENING_INPUT 이 가리키는 로컬 전용 URL 묶음, 커밋하지 않는다
screening-input.json
